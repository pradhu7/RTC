package com.apixio.model.cdi;

public enum ErrorType
{
    COL_COUNT,
    EMPTY,
    FORMAT,
    NOT_IN_ENUM,
    INVALID_CODE,
    CODE_LOOKUP,
    BEFORE,
    BOTH_EMPTY,
    BOTH_PRESENT,
    DUPLICATE,
    AMBIGUOUS,
    FILE_NOT_FOUND,
    RAPS_FORMAT_ERROR,
    MAO_FORMAT_ERROR,
    INVALID_PAA;
}
