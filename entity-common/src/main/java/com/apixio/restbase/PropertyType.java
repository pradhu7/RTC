package com.apixio.restbase;

/**
 * PropertyType defines the type of the extensible data that can be attached to a Customer
 * object.
 */
public enum PropertyType {
    STRING,
    BOOLEAN,
    INTEGER,
    DOUBLE,
    DATE
}
