################################################################
# Jedis configuration
jedisPool.host     = localhost
jedisPool.port     = 6379
jedisPool.timeout  = 2000
## NOTE:  I can't get passwords to work (which means I can't get database option either, since
## JedisPool constructor pattern is a bit messed up)
#jedisPool.password = foobared
#jedisPool.database = 0
jedisPool.queueBase  = unit_test_queues.
